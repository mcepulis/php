<?php

/*function textReplicator(string $text, ?int $count): string
{
    if ($count === null) {
        return $text;
    }

    $i = 1;
    $replicatedText = $text;
    while ($i < $count) {
        $replicatedText .= '-' . $text;
        $i++;
    }

    return $replicatedText;
}

var_dump(textReplicator('some_text', 2));*/

/*$textReplicator = function (string $text, ?int $count) {
    if ($count === null) {
        return $text;
    }

    $i = 1;
    $replicatedText = $text;
    while ($i < $count) {
        $replicatedText .= '-' . $text;
        $i++;
    }

    return $replicatedText;
};

var_dump($textReplicator('some_text', 2));*/
